{
  "technique_id": "T1552.001",
  "goal": "클라우드 관리 도구가 정상적인 AWS 자격 증명 설정 확인을 위해 credentials 파일을 읽는다.",
  "steps": [
    {
      "order": 1,
      "command": "mkdir -p /root/.aws && printf '[default]\\naws_access_key_id = FAKEKEY\\naws_secret_access_key = FAKESECRET\\n' > /root/.aws/credentials",
      "purpose": "실제 자격 증명이 아닌 테스트용 AWS credentials 파일을 준비한다."
    },
    {
      "order": 2,
      "command": "grep 'aws_access_key_id' /root/.aws/credentials > /dev/null",
      "purpose": "클라우드 관리 도구가 정상적인 설정 검증 과정에서 credentials 파일을 읽는다."
    }
  ]
}
