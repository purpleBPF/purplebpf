{
  "technique_id": "T1105",
  "goal": "설치 완료 후 정상 동작 확인을 위해 임시 post-check 도구를 실행한다.",
  "steps": [
    {
      "order": 1,
      "command": "cp /bin/true /var/tmp/pbpf-install-postcheck && chmod 755 /var/tmp/pbpf-install-postcheck",
      "purpose": "안전한 바이너리를 이용해 설치 검증 도구를 준비한다."
    },
    {
      "order": 2,
      "command": "/var/tmp/pbpf-install-postcheck",
      "purpose": "정상적인 운영 과정에서 설치 검증 도구를 실행한다."
    }
  ]
}
