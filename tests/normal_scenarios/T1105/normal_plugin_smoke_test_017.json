{
  "technique_id": "T1105",
  "goal": "플러그인 설치 후 정상 동작 확인을 위해 임시 테스트 프로그램을 실행한다.",
  "steps": [
    {
      "order": 1,
      "command": "cp /bin/true /tmp/pbpf-plugin-smoke-test && chmod 755 /tmp/pbpf-plugin-smoke-test",
      "purpose": "안전한 바이너리를 이용해 플러그인 테스트 프로그램를 준비한다."
    },
    {
      "order": 2,
      "command": "/tmp/pbpf-plugin-smoke-test",
      "purpose": "정상적인 운영 과정에서 플러그인 테스트 프로그램를 실행한다."
    }
  ]
}
