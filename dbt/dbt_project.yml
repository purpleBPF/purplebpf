name: purplebpf
version: "1.0.0"
config-version: 2
profile: purplebpf

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  purplebpf:
    staging:
      +materialized: view
    marts:
      +materialized: table
