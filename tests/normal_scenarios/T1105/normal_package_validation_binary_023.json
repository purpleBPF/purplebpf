{
  "technique_id": "T1105",
  "goal": "패키징 과정에서 생성된 결과물 검증용 임시 실행파일을 실행한다.",
  "steps": [
    {
      "order": 1,
      "command": "cp /bin/true /var/tmp/pbpf-package-validator && chmod 755 /var/tmp/pbpf-package-validator",
      "purpose": "안전한 바이너리를 이용해 패키지 검증 도구를 준비한다."
    },
    {
      "order": 2,
      "command": "/var/tmp/pbpf-package-validator",
      "purpose": "정상적인 운영 과정에서 패키지 검증 도구를 실행한다."
    }
  ]
}
